<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="@dimen/margin_small"
    app:cardCornerRadius="@dimen/card_corner_radius"
    app:cardElevation="2dp"
    app:strokeWidth="0dp"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- Dog Image -->
        <ImageView
            android:id="@+id/ivDogImage"
            android:layout_width="match_parent"
            android:layout_height="@dimen/friend_card_image_height"
            android:scaleType="centerCrop"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            tools:src="@drawable/img_app_logo" />

        <!-- Content Container -->
        <LinearLayout
            android:id="@+id/llContent"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="@dimen/margin_small"
            app:layout_constraintTop_toBottomOf="@+id/ivDogImage"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent">

            <!-- Hashtags -->
            <com.google.android.material.chip.ChipGroup
                android:id="@+id/chipGroupTags"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="@dimen/margin_small"
                app:singleLine="false"
                app:chipSpacing="4dp"
                app:chipSpacingHorizontal="4dp"
                app:chipSpacingVertical="4dp"
                app:selectionRequired="false"
                app:singleSelection="false">

                <!-- Sample Chips - will be populated dynamically -->
                <com.google.android.material.chip.Chip
                    android:id="@+id/chipSample1"
                    style="@style/Widget.Material3.Chip.Assist"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="#장난꾸러기"
                    android:textSize="10sp"
                    android:textColor="@color/gray_700"
                    app:chipBackgroundColor="@color/gray_100"
                    app:chipCornerRadius="@dimen/chip_corner_radius"
                    app:chipMinHeight="24dp"
                    app:textStartPadding="@dimen/chip_padding_horizontal"
                    app:textEndPadding="@dimen/chip_padding_horizontal"
                    app:chipStrokeWidth="0dp"
                    tools:visibility="visible" />

                <com.google.android.material.chip.Chip
                    android:id="@+id/chipSample2"
                    style="@style/Widget.Material3.Chip.Assist"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="#대인관계"
                    android:textSize="10sp"
                    android:textColor="@color/gray_700"
                    app:chipBackgroundColor="@color/gray_100"
                    app:chipCornerRadius="@dimen/chip_corner_radius"
                    app:chipMinHeight="24dp"
                    app:textStartPadding="@dimen/chip_padding_horizontal"
                    app:textEndPadding="@dimen/chip_padding_horizontal"
                    app:chipStrokeWidth="0dp"
                    tools:visibility="visible" />

            </com.google.android.material.chip.ChipGroup>

            <!-- Dog Name -->
            <TextView
                android:id="@+id/tvDogName"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="멍멍이"
                android:textColor="@color/gray_900"
                android:textSize="16sp"
                android:fontFamily="@font/pretendard_semibold"
                android:maxLines="1"
                android:ellipsize="end"
                tools:text="멍멍이" />

        </LinearLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView> 