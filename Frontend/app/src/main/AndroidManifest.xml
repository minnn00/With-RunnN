<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.With_Runn"
        tools:targetApi="31" >

        <meta-data
            android:name="com.naver.maps.map.NCP_KEY_ID"
            android:value="${naverMapClientId}"/>

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".FriendDetailActivity"
            android:exported="false" />

        <activity
            android:name=".AllFriendsActivity"
            android:exported="false" />

        <activity
            android:name=".ChatActivity"
            android:exported="false" />

        <activity
            android:name=".ChatRoomActivity"
            android:exported="false" />

        <activity android:name=".ui.onboarding.OnboardingActivity" />
        <activity android:name=".ui.onboarding.OnboardingLoginActivity" />
        <activity android:name=".ui.onboarding.OnboardingProfileActivity" />
        <activity android:name=".ui.onboarding.OnboardingProfileDefultActivity" />
        <activity android:name=".ui.onboarding.OnboardingProfilePersonalityActivity" />
        <activity android:name=".ui.onboarding.OnboardingProfileWalkingStyleActivity" />
        <activity android:name=".ui.mypage.MypageFollowersActivity" />
    </application>

</manifest>